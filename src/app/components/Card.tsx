import React from "react";
import { CardInterface } from "./types";

export const Card = ({
  body,
  badge,
  btn,
  image,
  subtitle,
  title,
}: CardInterface) => {
  return <article></article>;
};
